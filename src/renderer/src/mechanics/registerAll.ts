/**
 * Side-effect imports only: each module registers itself with
 * MechanicRegistry. Adding a new mechanic means adding one line here -
 * nothing else in the app needs to change.
 */
import './durability'
import './furniture'
import './noteblock'
import './stringblock'
import './armor-components'
import './armor-trims'
